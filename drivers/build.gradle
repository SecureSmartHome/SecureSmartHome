android {
    defaultConfig {
        //applicationId "de.unipassau.isl.evs.ssh.drivers"
    }
}

dependencies {
    compile project(':core')
}

android {
    defaultConfig {
        ndk {
            moduleName "ssh-drivers"
            ldLibs "log"
        }
    }
}