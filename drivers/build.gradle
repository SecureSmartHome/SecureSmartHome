android {
    defaultConfig {
        //applicationId "de.unipassau.isl.evs.ssh.drivers"
    }
}

dependencies {
    compile project(':core')
    //compile 'ODROID-libs'
}

android {
    sourceSets {
        main {
            jni.srcDirs = []
        }
    }
}