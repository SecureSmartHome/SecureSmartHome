<manifest
    package="de.unipassau.isl.evs.ssh.core"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <service android:name=".container.ContainerService"/>
    </application>

</manifest>
